# SAMPLE ENV FILE — copy to .env and fill values
# Never ever commit the real .env file with secrets, API keys, or credentials

TMDB_API_KEY=""
DATA_PATH="data/"
CACHE_PATH="cache/"
PORT=8501
DEBUG=True